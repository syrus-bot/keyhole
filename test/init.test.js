const assert = require("assert");
const sinon = require("sinon");

describe("test boilerplate", function () {
	it("should pass", () => {
		assert.ok(true);
	});

	it("should be modified", () => {
		assert.ok(false, "Unit testing must be initialised for this project.");
	});
});
